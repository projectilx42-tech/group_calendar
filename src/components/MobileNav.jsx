import React from 'react';
import { CalendarDays, Rows3, Users, Plus } from 'lucide-react';
export const MobileNav = ({ activeTab, setActiveTab, onAddEvent }) => <nav className="mobile-bottom-nav"><button className={`mobile-nav-item ${activeTab === 'calendar' ? 'active' : ''}`} onClick={() => setActiveTab('calendar')}><CalendarDays size={20} /><span>Kalendář</span></button><button className={`mobile-nav-item ${activeTab === 'timeline' ? 'active' : ''}`} onClick={() => setActiveTab('timeline')}><Rows3 size={20} /><span>Přehled</span></button><button className="mobile-nav-add-btn" onClick={onAddEvent} aria-label="Přidat dostupnost"><Plus size={22} /></button><button className={`mobile-nav-item ${activeTab === 'group' ? 'active' : ''}`} onClick={() => setActiveTab('group')}><Users size={20} /><span>Lidé</span></button></nav>;
