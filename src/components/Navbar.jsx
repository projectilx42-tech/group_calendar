import React from 'react';
import { CalendarDays, LogIn, LogOut, Plus } from 'lucide-react';
export const Navbar = ({ currentUser, onOpenAuth, onLogout, onAddEvent }) => <header className="glass-header"><div className="header-container"><div className="brand-logo"><div className="logo-icon"><CalendarDays size={18} /></div><span>Kdy spolu</span></div><div className="header-actions">{currentUser ? <><button className="btn btn-primary" onClick={onAddEvent}><Plus size={17} /> <span className="hide-mobile">Přidat dostupnost</span></button><button className="user-profile" onClick={onLogout} title="Odhlásit toto zařízení"><span className="user-avatar" style={{ background: currentUser.color }}>{currentUser.username.slice(0, 1).toUpperCase()}</span><span className="hide-mobile">{currentUser.username}</span><LogOut size={15} /></button></> : <button className="btn btn-primary" onClick={onOpenAuth}><LogIn size={16} /> Vstoupit</button>}</div></div></header>;
